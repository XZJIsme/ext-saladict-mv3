# Todo

- [x] 创建当前这个 Chrome Manifest V3 插件的最小可运行骨架，复用旧项目 icon；点击插件图标后弹出一个空白 popup，右上角有一个可关闭的叉。
- [x] 参考旧项目的翻译 source，给 popup 加一个句子输入框；按回车后渲染 `google`、`baidu`、`caiyun` 三个 source 的结果卡片，并完成最小可运行界面与基础请求框架。
- [x] 添加本地语言判定逻辑：如果输入以中文为主则默认翻译为英语；如果输入不是中文则默认翻译为中文，并为各 source 做目标语言映射。
- [x] 将 popup 视觉风格调整为类似 Windows Vista 的玻璃感界面，并保持中文文案。
- [x] 为当前插件设计并实现最小设置系统：参考旧项目的存储思路，但简化为统一的用户设置对象存储。
- [x] 新增独立设置页，并从 popup 标题栏提供“设置”入口；设置页要求未修改时保存按钮禁用，修改后启用。
- [x] 设置页第一版只提供“启用哪些翻译源”的选项，并保证至少勾选一个翻译源。
- [x] popup 按已保存的设置过滤翻译源卡片和实际请求，仅显示用户启用的 source。
- [x] 按旧项目的 `browser.*` 思路重构当前插件的扩展 API 访问层，移除业务代码中直接使用的 `chrome.*` 调用。
- [x] 继续研究并真正打通 `baidu` 翻译源，确认旧项目里的签名/请求路径在当前插件里的可行移植方式。
- [x] 继续研究并真正打通 `caiyun` 翻译源，确认 token 方案与旧项目 fallback 逻辑是否值得迁移。
- [x] 在设置页为彩云翻译增加 token 输入框，并在启用彩云但 token 为空时于 popup 明确提示用户先填写 token。
- [x] 仿照彩云翻译的设置方式，为 `baidu` 增加 token 输入框，并在启用百度翻译但 token 为空时于 popup 明确提示用户先填写 token。
- [x] 让 token 设置直接显示在对应翻译源卡片下方，并且未启用时也可以先输入和保存 token。
- [ ] 增加 `zhipu` 翻译源。
- [x] 增加未启用的翻译源也可以临时启用一下的能力。
- [x] 参考旧项目的 pinned 逻辑，在 MV3 里增加“锁定”入口；点击后切换到可保持打开的独立扩展窗口，并显示明确的锁定状态。
- [ ] 计划多语言支持，至少展示界面里的文案可以切换成其他目标语言。
- [x] 翻译结果支持收起某个源。
- [x] 增加多主题设置（WindovvViista、Mojavv）。
- [ ] 增加其它主题（Snow、Nori、Ocean、Violet、Bacon、Coffee、Sakura）。
- [ ] Dark 模式（Midnight、Blackcoffee）。
