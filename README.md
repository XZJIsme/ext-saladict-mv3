# Saladict MV3

基于深受喜爱的 [Saladict MV2](https://github.com/crimx/ext-saladict) 版本，使用 Manifest V3 重构的 Chrome 扩展。目前功能较为简单，用于查词、翻译等基础功能。

## 在 Chrome 里运行

1. 在 Chrome 地址栏打开 `chrome://extensions`
2. 打开右上角的 `Developer mode`
3. 点击 `Load unpacked`
4. 选择当前项目根目录
5. 不要选 `old-salad-archive`
